<% // Variáveis recebidas: produto const
    badgeClasses="absolute top-4 left-4 px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wider" %>

    <article
        class="bg-white rounded-xl shadow-md overflow-hidden flex flex-col items-start w-full group transition-transform hover:-translate-y-1 hover:shadow-lg focus-within:-translate-y-1 focus-within:shadow-lg">

        <div class="relative w-full aspect-[4/3] bg-slate-100 overflow-hidden">
            <img src="<%= produto.imagem %>" alt="Foto de <%= produto.nome %>"
                class="w-full h-full object-cover origin-center transition-transform duration-500 group-hover:scale-105 <%= !produto.em_estoque ? 'grayscale opacity-75' : '' %>"
                <% if (typeof index !=='undefined' && index < 2) { %> fetchpriority="high" <% } else { %> loading="lazy"
                <% } %>>
                    <% if (!produto.em_estoque) { %>
                        <div class="absolute top-4 left-4 flex flex-col gap-2">
                            <div class="px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wider bg-slate-800 text-white shadow-sm flex items-center gap-1"
                                aria-label="Produto Temporariamente Indisponível">
                                Esgotado
                            </div>
                        </div>
                        <% } else { %>
                            <div class="absolute top-4 left-4 flex flex-col gap-2">
                                <% if (!produto.contem_gluten) { %>
                                    <div class="px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wider bg-ambar text-marinho shadow-sm"
                                        aria-label="Produto sem glúten">
                                        Sem Glúten
                                    </div>
                                    <% } else { %>
                                        <div class="px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wider bg-red-600 text-white shadow-sm flex items-center gap-1"
                                            aria-label="Produto contém glúten">
                                            ⚠️ Contém Glúten
                                        </div>
                                        <% } %>

                                            <% if (produto.alergenos && produto.alergenos.includes('ovos')) { %>
                                                <div class="px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wider bg-orange-600 text-white shadow-sm flex items-center gap-1"
                                                    aria-label="Produto contém ovos">
                                                    🥚 Contém Ovos
                                                </div>
                                                <% } %>
                            </div>
                            <% } %>
        </div>

        <div class="p-5 flex flex-col flex-grow w-full">
            <div class="flex items-center justify-between w-full mb-2">
                <span class="text-xs font-semibold text-slate-500 uppercase tracking-widest">
                    <%= produto.categoria==='couro' ? '🐟 Couro' : (produto.categoria==='escama' ? '🐠 Escama'
                        : '🦐 Frutos do Mar' ) %>
                </span>
            </div>

            <h2 class="font-titulo text-xl font-bold text-marinho mb-2 line-clamp-1">
                <%= produto.nome %>
            </h2>
            <p class="text-sm text-slate-600 mb-4 line-clamp-2">
                <%= produto.descricao_seo %>
            </p>

            <!-- Spacer para empurrar preço e botão pro final do card -->
            <div class="mt-auto flex items-center justify-between w-full">
                <div class="flex flex-col">
                    <span class="text-xs text-slate-600">A partir de</span>
                    <span class="font-bold text-lg <%= produto.em_estoque ? 'text-esmeralda' : 'text-slate-400' %>">
                        <%= formatters.formatCurrency(Object.values(produto.preco_por_peso)[0]) %>
                    </span>
                </div>

                <a href="/peixe/<%= produto.slug %>"
                    class="bg-marinho text-white px-5 py-2.5 rounded-lg text-sm font-semibold transition-colors hover:bg-slate-700 focus:ring-2 focus:ring-offset-2 focus:ring-marinho focus:outline-none min-h-[44px] min-w-[44px] flex items-center justify-center"
                    aria-label="Ver detalhes de <%= produto.nome %>">
                    Detalhes &rarr;
                </a>
            </div>
        </div>
    </article>